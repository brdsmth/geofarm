/**
 * @geofarm/access
 * RFC-0002: grants, capabilities, sub-worlds
 * Placeholder until M2/P-18..P-19 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/access" as const;
