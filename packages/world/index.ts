/**
 * @geofarm/world
 * RFC-0001/0003/0004: the vocabulary as types
 * Placeholder until M1/P-14 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/world" as const;
