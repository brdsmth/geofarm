/**
 * @geofarm/feeds-weather
 * RFC-0011, RFC-0016 I1: weather external Actor
 * Placeholder until M4/P-29 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/feeds-weather" as const;
