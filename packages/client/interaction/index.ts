/**
 * @geofarm/client-interaction
 * RFC-0006: five verbs, promotion gate
 * Placeholder until M3/P-26 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/client-interaction" as const;
