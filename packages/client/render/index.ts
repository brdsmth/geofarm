/**
 * @geofarm/client-render
 * RFC-0005/0015: marks, lenses, invalidation
 * Placeholder until M3/P-25 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/client-render" as const;
