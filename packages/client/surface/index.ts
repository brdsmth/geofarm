/**
 * @geofarm/client-surface
 * RFC-0000 §2.6: the vocabulary membrane — all user-facing strings
 * Placeholder until M3/P-28 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/client-surface" as const;
