/**
 * @geofarm/journal
 * RFC-0004/0012/0013: admission, immutability, the walk
 * Placeholder until M1/P-15..P-17 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/journal" as const;
