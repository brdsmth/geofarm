/**
 * @geofarm/projection
 * RFC-0003/0004/0007/0009: every derived reading
 * Placeholder until M2/P-20 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/projection" as const;
