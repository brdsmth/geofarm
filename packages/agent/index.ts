/**
 * @geofarm/agent
 * RFC-0009/0010: context, modes, evidence
 * Placeholder until M5/P-32..P-37 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/agent" as const;
