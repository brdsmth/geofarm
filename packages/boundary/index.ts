/**
 * @geofarm/boundary
 * RFC-0012: Project/Append/walk, the only door
 * Placeholder until M3/P-23 — PLAN-001 §3 scaffold; no functionality is
 * implemented before its milestone (M0 scope: compile + test harness only).
 */
export const PACKAGE = "@geofarm/boundary" as const;
